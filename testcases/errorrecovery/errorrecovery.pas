program exLocal
const
   low = 10
type 
   int=integer
var
   a, b, c: int
function display(x, y:integer;var z: real):integer
var
   a, b, c: integer
begin
   b := (20 + 10.0) * a
   begin
      a:=3;
   end
end;
begin
   if((a=a)=(b=b)) begin
      a:=-a;
end
